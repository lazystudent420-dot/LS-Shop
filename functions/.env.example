# Copy to functions/.env.YOUR_PROJECT_ID only after configuring your sender.
FUNCTIONS_REGION=us-central1
ENABLE_REMINDER_EMAILS=false
EMAIL_FROM=
# EMAIL_API_KEY is a Secret Manager secret, never a committed environment value.
